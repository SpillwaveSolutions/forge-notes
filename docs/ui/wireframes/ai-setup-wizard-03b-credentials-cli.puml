' Source: src/components/ai/AiSetupWizard.tsx:378 CliCredentialsStep
' State: data-wizard-step="credentials", backend IS a CLI (isCliBackend branch).
' A genuinely different screen, not a variant — hence its own wireframe.
@startsalt
{+
  {* AI setup · Deep Agents & coding CLIs | X }
  --
  { 1. Welcome | 2. Provider | <b>3. Credentials | 4. MCP tools | 5. Skills | 6. Test & finish }
  ==
  <b>Claude Code CLI
  This backend shells out to the CLI on the host. No API key is stored here.
  .
  { <b>Binary | `claude`   [ on PATH ] }
  { <b>Auth   | Uses the CLI's own login — run `claude login` if unauthenticated. }
  .
  <i>Desktop only: the browser build cannot spawn processes.
  --
  { [Back] | . | [Continue] }
}
@endsalt

node_modules/
.project_id
.github_repo
.env
.env.*

# worklog wiki publish cache
.work/wiki-checkout/

# python bytecode
__pycache__/
*.pyc

# test output
playwright-report/
test-results/

# machine-local port assignments (see scripts/dev-ports.mjs)
.dev-ports.json
screenshots/ui/

# Generated by `npm run build:desktop` from .vercel/output/static — the
# Tauri bundler embeds these into the binary, so they are a build artifact,
# not a source. 126 files / 4.4 MB slipped in with the icon fix.
dist-desktop/
